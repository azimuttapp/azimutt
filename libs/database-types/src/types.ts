export type SqlScript = string // a whole SQL script with several queries or even broken parts inside
export type SqlQuery = string // a single and complete SQL query
export type SqlFragment = string // a part of a SQL query, can be used to build a bigger query
